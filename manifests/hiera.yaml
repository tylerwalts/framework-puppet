---
:hierarchy:
  - "hosts/%{::fqdn}"
  - "roles/%{::hostname}"
  - "domains/%{::domain}"
  - project
  - common

:backends:
  - yaml 
  - json

:json: &data_dir
  :datadir: "%{::puppet_cwd}/manifests/config"

:yaml: *data_dir
