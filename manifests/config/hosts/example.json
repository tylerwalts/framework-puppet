/*

This directory should contain a file for a specific hostname.
Use this FQDN scope as last resort, for outlier use cases.

Can add new things here, or over-ride values previously loaded in the hierarchy.

Example Files:
  my.fully.qualified.domain.name.com.json
  host.vagrant.domain.com.json

Example Contents:

  {
    "overridden::value": "value1"
  }

*/

