// This is intended to ensure common resource that we use on every project.
// Avoid customizing this file, and use the project, domain and hosts config.
{
  "classes":[
    "general"
  ],

  "general::ensure":"latest"
}
